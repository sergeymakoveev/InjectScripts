{
  "name": "InjectScripts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prepare": "shx mkdir -p dist/icons",
    "clean": "shx rm -rf dist & shx rm -f *.zip",
    "copy": "npm run prepare & shx cp src/*.json dist & shx cp src/*.html dist & shx cp src/icons/*.png dist/icons",
    "build": "npm run clean & npm run copy & babel src --out-dir dist --presets=@babel/preset-env",
    "pack": "npm run build & zip inject-scripts.zip dist",
    "pack-extension": "chrome --pack-extension=dist --no-message-box"
  },
  "browserslist": "> 0.25%, not dead",
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "shx": "^0.3.2"
  }
}
